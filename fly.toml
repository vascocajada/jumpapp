# fly.toml app configuration file generated for your-app-name-white-darkness-7193 on 2025-07-20T21:14:12+01:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'your-app-name-white-darkness-7193'
primary_region = 'cdg'

[build]
  dockerfile = 'Dockerfile'

[env]
  APP_ENV = 'prod'

[[services]]
  protocol = 'tcp'
  internal_port = 8080

  [[services.ports]]
    port = 80
    handlers = ['http']

  [[services.ports]]
    port = 443
    handlers = ['tls', 'http']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
